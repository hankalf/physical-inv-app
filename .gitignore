node_modules/
# The whole data directory is runtime state: the live database, its -wal/-shm
# side files, and the daily backups - which hold counts and password hashes.
# In production it is a mounted volume; locally it is nobody else's business.
data/
.env
.DS_Store
tests/screenshots/
